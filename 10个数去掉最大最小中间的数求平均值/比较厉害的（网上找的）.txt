#include<stdio.h>
int main()
{
  float a[10],max,min,t,sum=0;
  int i;
  for(i=0;i<10;i++)
   scanf("%f",&a[i]);
    max=min=a[0];
    for(i=1;i<10;i++)
     {
	 if(a[i]>a[i-1]) max=a[i];
	 if(a[i]<a[i-1]) min=a[i];
	 sum+=a[i];
     }
 sum=sum+a[0]-max-min;
  t=sum/8;
  printf("%f",t);
   	return 0;
	
}


